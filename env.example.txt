# =============================================================================
# OneDevelopment Agent - Environment Configuration Template
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file to .env in the project root directory
# 2. Copy the frontend section to frontend/.env
# 3. Fill in your actual values (replace all <YOUR_...> placeholders)
# 4. NEVER commit .env files to version control!
# =============================================================================

# ============== SERVER CONFIGURATION ==============
# Your server's public IP address or domain name
# Examples:
#   - For AWS EC2: Use your Elastic IP or instance public IP
#   - For local dev: Use localhost
#   - For production: Use your domain name
SERVER_IP=<YOUR_SERVER_IP>
FRONTEND_URL=http://<YOUR_SERVER_IP>:3000
BACKEND_URL=http://<YOUR_SERVER_IP>:8000

# ============== DJANGO CONFIGURATION ==============
SECRET_KEY=django-insecure-change-this-in-production
DEBUG=False

# Comma-separated list of allowed hosts
# Include: localhost,127.0.0.1,your-server-ip,your-domain.com
ALLOWED_HOSTS=localhost,127.0.0.1,<YOUR_SERVER_IP>

# Comma-separated list of CORS allowed origins
# Include all URLs that will access your backend
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://<YOUR_SERVER_IP>:3000,http://<YOUR_SERVER_IP>:8000

# ============== DATABASE CONFIGURATION ==============
DB_NAME=onedevelopment_agent
DB_USER=postgres
DB_PASSWORD=<YOUR_DB_PASSWORD>
DB_HOST=localhost
DB_PORT=5432

# ============== OPENAI API ==============
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=<YOUR_OPENAI_API_KEY>

# ============== REDIS CONFIGURATION ==============
REDIS_URL=redis://localhost:6379/0

# ============== AVATAR SERVICE ==============
# Avatar service URL (e.g., ngrok tunnel for local GPU processing)
# This should point to your avatar generation service
# Examples:
#   - ngrok: https://abc123.ngrok-free.app
#   - AWS GPU instance: http://your-gpu-instance-ip:8000
#   - Local: http://localhost:8000
AVATAR_SERVICE_URL=<YOUR_AVATAR_SERVICE_URL>

# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# Create frontend/.env with these variables:
# =============================================================================

# ============== BACKEND API URL ==============
# Point this to your backend API endpoint
# Examples:
#   - Local development: http://localhost:8000/api
#   - AWS production: http://<YOUR_SERVER_IP>:8000/api
#   - Custom domain: https://api.yourdomain.com/api
REACT_APP_API_URL=http://localhost:8000/api

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Never commit .env files to version control
# 2. Keep your SECRET_KEY secret and unique per environment
# 3. Set DEBUG=False in production
# 4. Use strong database passwords in production
# 5. Regularly rotate API keys and secrets
# 6. Use HTTPS in production (SSL/TLS certificates)
# 7. Keep OPENAI_API_KEY secure - monitor usage to detect leaks
# 8. Use environment-specific .env files (dev, staging, prod)
# 9. Consider using AWS Secrets Manager or similar for production
# 10. Never hardcode credentials in source code

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# AWS Deployment:
#   1. Set SERVER_IP to your Elastic IP address
#   2. Add your Elastic IP to ALLOWED_HOSTS
#   3. Add frontend URLs to CORS_ALLOWED_ORIGINS
#   4. Use RDS for database in production
#   5. Use ElastiCache for Redis in production

# Local Development:
#   1. Set SERVER_IP=localhost or 127.0.0.1
#   2. Keep DEBUG=True for development
#   3. Use local database and Redis instances
#   4. Use ngrok for AVATAR_SERVICE_URL if testing avatar features

